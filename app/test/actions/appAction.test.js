/* eslint-disable no-undef */

import expect from 'expect';

describe('AppAction', () => {
    describe('getAppName', () => {
        it('should return the app name', () => {
            expect(1).toEqual(1);
        });
    });
});
